What is Hashing?
It basically converts a peice of 
string to larger INTEGER type number.

Lets say we have "Adhiraj"-> give some random number
 like 8473992 which is really large.

 Lets say we want to make an array of a size 10 
 so 8473992 will not have a place to fit.

 for e.g arr = {"akofk","hile","ili","ioee","aoiu"} in this
  we cannot fit anything with that index.

  SO therefore we take MODULO of
that number with size of that array.

like 8473992%10 = 5
so lets say we took "hiesw" it will get index 5.
arr = {"akofk","hile","ili","ioee","aoiu","hiesw"}
          0       1     2      3      4       5
lets say we took size as M so usually we take 
M as a prime number so thatit does not give
 any number in the multiple of 2 or 10 which is common.

 